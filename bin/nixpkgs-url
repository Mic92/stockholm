#! /bin/sh
# nixpkgs-url : nixos-config -> git_url
set -euf
nix-instantiate \
    -I nixos-config="$1" \
    --eval \
    --json \
    -E \
    '
      (import <nixos-config> {config={}; pkgs={};}).nixpkgs.url
    ' \
    2> /dev/null \
  | jq -r . 2> /dev/null
